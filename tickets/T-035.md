---
id: T-035
title: Export duel tray capacity (TRAY_CAPACITY) in tuning
status: in-progress
wave: 5
depends_on: [T-004]
branch: ticket/T-035-tray-capacity
file_scopes:
  - src/engine/tuning.ts
test_scopes:
  - __tests__/engine/tray-capacity.test.ts
github_issue: null
model_hint: standard
attempts: 0
traces_to:
  - PLAN.md §The armory (duel tray shows a subset of owned cannons)
  - tickets/T-030.md (loadout selection; tray capacity 3)
  - tickets/app/A-011.md (gun deck blocked on engine TRAY_CAPACITY)
---

## Context

App ticket A-011 (gun deck) needs the duel-tray slot count from the engine, not a UI literal.
T-030 owns loadout _selection_ rules but is blocked on owner open questions. This ticket ships
only the constant T-030 already planned for `tuning.ts` so A-011 can proceed.

Verified design number: **3** (T-030 Context: "duel tray capacity: 3"; design board 4d).

## Planning Decisions

- `locked-decision` — `TRAY_CAPACITY = 3` lives in `src/engine/tuning.ts` (ARCHITECTURE §4.3).
- `proposed` — selection / validation logic stays in T-030 (`src/engine/loadout.ts`); this ticket
  does not invent equipped-set rules.

## Acceptance Criteria

- **AC-1**: Given the tuning module, when imported, then it exports `TRAY_CAPACITY` as the
  integer `3`.
- **AC-2**: Given `src/engine/tuning.ts` source text, when scanned, then the `TRAY_CAPACITY`
  export is accompanied by a comment citing PLAN.md / the tray-capacity design source (not a
  bare magic number with no provenance).
- **AC-3**: Given the constant, when used as a slot bound, then it is a positive integer
  strictly less than the catalog cannon count (10) — a tray that holds every gun is not a tray.

## Test Plan

- Unit (deterministic): AC-1…AC-3 in `__tests__/engine/tray-capacity.test.ts`.

## Definition of Done

- [ ] Every AC has a passing test tagged `spec(T-035:AC-n)`
- [ ] `.tdd-swarm/run-local-gates.sh` is green
- [ ] `.tdd-swarm/spec-lint.sh tickets/T-035.md` is green
- [ ] Every DoD item above has a passing test tagged `dod(T-035:n)`, numbered in file order
- [ ] Files changed are exactly those in `file_scopes` (plus this ticket's test file)

## Orchestrator adjudication (2026-07-29)

- Split from T-030 to unblock A-011. T-030 DoD-1 becomes "consumes `TRAY_CAPACITY` from T-035".

## Out of Scope

- `src/engine/loadout.ts` and equipped-set rules — T-030.
- App gun-deck UI — A-011.
- Resolving T-030 open questions (mid-duel change; above-band equip).
