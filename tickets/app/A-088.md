---
id: A-088
title: The chart rebuilt — sixteen places, the sixth station, one screen one scale
status: backlog
wave: G14
depends_on: [A-082]
branch: main
file_scopes:
  - app/chart.tsx
  - src/components/chart/board.ts
  - src/components/chart/VoyageMap.tsx
  - src/components/chart/Station.tsx
  - src/components/chart/Sea.tsx
  - src/components/chart/Isle.tsx
  - src/components/chart/Fog.tsx
  - src/services/chart.ts
test_scopes:
  - __tests__/app/design-fidelity.test.ts
  - __tests__/app/chart.test.ts
  - __tests__/app/chart-progress-presentation.test.ts
owner: app
---

## Ruling

**D-19** (read it — it names the mechanics, the frozen consequences, and the sequencing
law). **DO NOT START until wave G13 is integrated and committed** — this ticket moves pins
A-082 holds green. Board = pixel authority: `Cannon Academy Sea Chart.dc.html` (the "TURN 9
rebuilt" version, etag 1785769722668416+) in project `88888c12-22e4-4781-b76f-a28110506499`.

## The work — CORRECTED SCOPE (see D-19's premise correction)

The sixteen-place chart is ALREADY IMPLEMENTED (isles, waypoint silhouettes, derived trails,
kraken, compass, gated counter — the original transcription shipped it all). This ticket
adds ONLY the board's genuinely new material:

1. **The sixth Uncharted station** (storm blobs, ghost ship, bolts, compass-? marker, "The
   Uncharted Sea" name pill) in its three D-19 states wired to real state — fogged from the
   game's first minute ("BEAT THE GRANDLINE FIRST"), beckoning at chain completion ("SAIL
   PAST THE EDGE"), returned with the `uncharted.clearedCount` pennant tally — the same data
   A-082's dock chip reads; tapping the live station dispatches the same `chart-uncharted`
   edge as the chip.
2. **The dashed grandline→uncharted trail leg** through the existing derived-dots algorithm
   (dashed, 46px dip, per the board script).
3. The station joins the counter honestly if the owner wants a 17th place — otherwise the
   counter law stays at 16; state which in the report.

Dock unchanged — A-082's chip and verbs stay byte-identical. Everything already implemented
stays byte-identical.

## Acceptance criteria

- **AC-1**: Transcription pins re-baselined to the new board citing D-19 — new frame
  arithmetic, isle positions, station geometry — with the same measured-not-invented
  discipline; the old 664-extent pins retire in the same commit, header-cited.
- **AC-2**: The station's three states render from real captain state, and the fogged state
  is visible for a FRESH captain (the day-one locked door), swept across all three bands.
- **AC-3**: Trails are derived: a test moves one island's centre constant and the leg's dots
  provably follow (then reverts) — the board's "route can never disagree" law.
- **AC-4**: chartNodes still returns exactly five islands; no gen id, no waypoint id enters
  it (the D-19 restatement of the bus law); `chart-progress-presentation` dock specs stay
  green untouched.
- **AC-5**: Only islands and the live station are tappable; kraken/rocks/compass have no
  press handlers (source-scan); every hit target ≥64×64 per the board's hit-step note.

## Constraints

Zero new rasters (ship-01 + cannon.png only, already pinned). Terrain/deep-sea tokens as
declared by the boards (A-082 already landed the deep-sea group — reuse, don't redeclare).
D-13: no skip affordances. Reanimated: no closures in worklets.

## Verification required

`npx tsc --noEmit` clean. Targeted single-fork: the three test_scopes green. Mutation: AC-3's
follow-the-centre check; unpinning the station's fogged-from-day-one state must redden AC-2.

## Out of scope

Waypoint interactivity and found-state (A-089), wreck/rival-at-sea duels (A-090), dock, the
Uncharted Sea screen.
