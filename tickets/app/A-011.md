---
id: A-011
title: Gun deck — which three cannons sail with you
status: backlog
wave: A5
depends_on: [A-001]
branch: ticket/a-011
file_scopes:
  - app/gun-deck.tsx
test_scopes:
  - __tests__/app/gun-deck.test.ts
github_issue: null
model_hint: standard
attempts: 0
traces_to:
  - Design board 4d (gun deck — 6 owned, 3 slots)
  - T-030 (loadout selection)
  - Design review 2026-07-28 ("nothing decides which three")
---

## Context

By the Grandline a captain owns up to ten cannons and the duel tray holds three. Nothing decides
which three. T-030 is the engine rule; this is the screen that drives it.

It already bites at MVP scale: `resolvePlacement('k_1')` returns **four** cannons today, which is
why the tray had to become scrollable. Board 4d is the designed answer.

## Acceptance Criteria

- **AC-1**: Given more owned cannons than slots, when the deck opens, then every owned cannon is
  shown and exactly the equipped ones are marked.
- **AC-2**: Given three equipped cannons, when a fourth is equipped, then the player must displace
  one — the deck never silently drops a choice.
- **AC-3**: Given an equipped set, when it is committed, then it persists and the duel tray shows
  exactly it, in catalog order.
- **AC-4**: Given an attempt to equip zero cannons, when committed, then it is refused.
- **AC-5**: Given a newly unlocked cannon, when the deck opens, then it is marked as new until seen.

## Test Plan

- Unit (deterministic): every AC against T-030's loadout rules and the store.

- Visual (design-fidelity, NOT frozen): verified against the named board at 375pt and at
  360×640 / 390×844 / 430×932, screenshot evidence attached. **Posture-gated deferral** — no
  component-test harness exists for the app layer (`.tdd-swarm/posture.md`).

## Definition of Done

- [ ] All acceptance criteria have a passing test written by the Test Agent
- [ ] Local gates green
- [ ] Slot count comes from the engine, not a literal here
- [ ] Reviewer approved

## Out of Scope

Double-Shot arming (board 4e — separate ticket). Cannon stats rebalancing.
