---
id: T-031
title: Resolve the Perfect Shot "bonus ball" contradiction
status: backlog
wave: null
depends_on: [T-004]
branch: ticket/T-031-perfect-shot-semantics
file_scopes:
  - ARCHITECTURE.md
  - src/engine/tuning.ts
test_scopes:
  - __tests__/engine/tuning.test.ts
github_issue: null
model_hint: cheap
attempts: 0
traces_to:
  - ARCHITECTURE.md §4.3 ("perfectShot (elapsed < 40% of timer) → +1 bonus ball")
  - ARCHITECTURE.md §4.3 ("Damage renders as N cannonball arcs ... so the roll reads as shot spread")
  - Design review, 2026-07-28 (external designer — "my prototype shows a '+1 BALL' chip, which is currently a lie")
  - .tdd-swarm/reports/T-004-code-review.md (independently flagged BASE_BALLS_PER_VOLLEY = 1)
---

## Context

Two statements in ARCHITECTURE §4.3 are not satisfiable by the shipped constants:

```
ARCHITECTURE:202  "perfectShot → +1 bonus ball"
tuning.ts:89      PERFECT_SHOT_BONUS_DAMAGE = 1      <- +1 DAMAGE, not a ball
ARCHITECTURE:206  "Damage renders as N cannonball arcs, so the roll reads as shot spread"
tuning.ts:86      BASE_BALLS_PER_VOLLEY = 1          <- one arc; no spread can read
```

The external design review caught the first: a prototype showing a "+1 BALL" chip advertises a
mechanic the engine does not implement. The T-004 code review independently flagged the second as a
Minor.

**This must resolve before T-008 (wave 3) builds the damage model**, which would otherwise bake in
whichever reading it happens to infer.

## Planning Decisions

- `proposed` (orchestrator, 2026-07-28) — **damage is scalar; balls are presentation.**
  - `PERFECT_SHOT_BONUS_DAMAGE` stays `+1` **damage**.
  - `BASE_BALLS_PER_VOLLEY` is reclassified as a **rendering constant the engine ignores** — how
    many arcs to draw for one volley. Damage does not subdivide across it, and drawing an extra arc
    adds no damage.
  - ARCHITECTURE §4.3's "+1 bonus ball" wording is **corrected** to "+1 damage, celebrated with an
    extra cannonball arc", so doc and code agree.

  **Rationale:** the bonus was never where the reward lives. On the Swivel (8–12), a fast answer
  already biases the roll from 8–10 up to 11–12 through `QUALITY_WEIGHT` — roughly a **60% swing**,
  and that is what a child actually feels. The `+1` is garnish. Calling it a "ball" implies a
  mechanic that does not exist and sets a designer up to advertise it in the UI.
- `open-question` — the arc count for legibility is the designer's call, not the engine's. Under
  this ruling, choosing three arcs so the spread reads is a pure presentation change with no engine
  impact.

## Acceptance Criteria

- **AC-1**: Given `ARCHITECTURE.md` §4.3, then no sentence claims a Perfect Shot adds a projectile
  that carries damage; the corrected wording states `+1` damage and describes the extra arc as
  presentation.
- **AC-2**: Given `tuning.ts`, then `BASE_BALLS_PER_VOLLEY` carries a comment stating it is a
  presentation constant with no effect on any damage computation, and naming who may read it (the
  duel view, not the engine).
- **AC-3**: Given `PERFECT_SHOT_BONUS_DAMAGE`, then its value and type are unchanged — this ticket
  changes documentation and classification, not behaviour.
- **AC-4**: Given the full repo suite, then it passes unchanged; no existing assertion moves.
- **AC-5**: Given a search of `src/engine/**` after T-008 lands, then no damage computation reads
  `BASE_BALLS_PER_VOLLEY`.

## Definition of Done
- [ ] ARCHITECTURE §4.3 corrected on both sentences
- [ ] `BASE_BALLS_PER_VOLLEY` reclassified in-code with a comment
- [ ] No behaviour change; suite green and unchanged
- [ ] **T-008's brief updated to state the ruling** so the damage model cannot infer the other reading

## Out of Scope
- Changing `PERFECT_SHOT_BONUS_DAMAGE`'s value. If the owner later wants a *perceptible* flat bonus,
  that is a balance decision for the dev screen, not a semantics fix.
- The Perfect Shot VFX itself.
