# A-018 web chart smoke — PASS

- Tested commit: `74c42ecf6443a2199075371d7971d2829ebbb1ef`
- Source-code tip within that commit: `81ccba9`
- Server: `CI=1 npx expo start --web --port 8092`
- Browser viewport: `window.innerWidth=375`, `window.innerHeight=812`, DPR `1`
- Route reached through UI: onboarding → name/flag → guided-duel auto-advance → `/chart`

## Render evidence

The rebuilt chart rendered with:

- captain/ship name `Sea Star`
- current ship and `YOU ARE HERE`
- all five stations
- two fogged stations
- coin pill
- Fight, Practice, and Gun deck controls

Screenshot:
`2026-07-29T1032-web-chart-375x812-74c42ec.png`

## Route evidence

| Action   | Destination                                                  | Return                      |
| -------- | ------------------------------------------------------------ | --------------------------- |
| Fight    | `/duel`, hull bars and both equipped cannons visible         | anchor returned to `/chart` |
| Practice | `/range`, four skill drills visible                          | Leave returned to `/chart`  |
| Gun deck | `/gun-deck`, two equipped cannons and one empty slot visible | Done returned to `/chart`   |

## Runtime evidence

- Page/console errors: **0**
- Warnings: two identical React Native Web deprecations:
  `props.pointerEvents is deprecated. Use style.pointerEvents`
- Unmatched route, red screen, or blank render: **none**

The warning is non-blocking and unrelated to a Reanimated worklet boundary. The chart worklets
animated while the page remained interactive.
